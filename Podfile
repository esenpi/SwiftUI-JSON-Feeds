# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PBMod' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PBMod
  pod 'SwiftyJSON', '~> 5.0'
  pod 'Kingfisher', '~> 7.3'
  pod 'AlertToast'

  target 'PBModTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PBModUITests' do
    # Pods for testing
  end

end
